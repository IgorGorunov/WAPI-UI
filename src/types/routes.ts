export const enum Routes {
  Dashboard = "/",
  Login = "/login",
  Orders = "/orders",
  Products = "/products",
  Inbounds = "/inbounds",
  StockMovements = "/stockMovements",
  Outbounds = "/outbounds",
}
