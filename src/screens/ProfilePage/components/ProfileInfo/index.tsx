import React from "react";

import "./styles.scss";

const ProfileInfo: React.FC = () => {

    return (
        <div className={`profile-info`}>
            Here is profile info
        </div>
    );
};

export default ProfileInfo;
